启动与停止线程

threading库可以在单独的线程中执行任何的在python中可以调用的对象

Thread对象并将你要执行的对象以target参数的形式提供给该对象

python中的线程会在一个单独的系统级线程中执行，这些线程将由操作系统来全权管理

线程一旦启动，将独立执行知道目标函数返回

查询一个线程对象的状态，可以使用is_alive()方法

join()方法用于将一个线程加入到当前线程，并等待它终止