尽量用enumerate取代range

在一系列整数上面迭代时，内置的range函数很有用

对于字符串列表这样的序列式数据结构，可以直接在上面迭代

python提供了内置的enumerate函数，可以把各种迭代器包装
成生成器，以便稍后产生输出值

生成器每次产生一对输出值，其中，前者表示循环下标，
后者表示从迭代器中获取到的下一个序列元素

可以给enumerate提供第二个参数，以指定开始记数时所用的值，默认为0
